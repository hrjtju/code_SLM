
**待办事项**

- [ ] 搭建SLM优化RL代码
    - [x] 修复未同步的代码
    - [ ] 按照会议反馈重写环境
        - [x] 补充零件信息作为网络输入（Theoretical）
        - [ ] 调整slm_class的诸类方法，使得在环境定义时更加方便
            - [ ] 在metadata类中添加get_part_vec方法，输出某类零件的特征向量
            - [ ] 在metadata类中添加计算$P_{\text{mask}}$的类方法，在装在好数据之后立即调用
            - [ ] 
    - [ ] 解决2d-bin-packing的问题
    - [ ] 实现Batch类和Batch机制
    - [ ] 解决和joy-RL框架适配的问题
    - [ ] 开始训练
- [ ] 撰写 Problem Formulation

**问题**
* 增强网络输出适应不同情况的能力
* **将算力变成图**

## 环境定义

* 状态：所有零件的参数（A）、剩余量和该Batch占用情况的离散化版本（B）
    * 状态中需要包含json文件中每个零件的所有信息，每个零件的状态向量固定为$1 + 2 + 4 \times 7 = 31$维，其中零件数量一维，零件本身参数（体积和表面积）两维，剩余为每个打印方向的四个数据（长宽高、支撑体积）乘以打印方向总数。并同样使用$0$填补不适用的打印方向和零件。如只有$10$个零件，那么状态矩阵的后$20$行均为零，假设某零件只有$2$个打印方向，则矩阵中该行的后$4 \times 5 = 20$维分量均为零。
    * 使用固定解析度（如$224 \times 224$）将该Batch进行离散化作为输入网络的第二个参数
* 动作：选择零件的类别和摆放的方向。
    * 为了网络鲁棒性，可暂时将零件类别数和摆放方向开大，例如均以算例中的最大值设定：$v_{\text{parts}} \in \mathbb{R}^{20}$, $v_{\text{ori}} \in \mathbb{R}^{7}$，以适应所有零件类别和摆放方向小于对应值的设定。
    * 对于零件类别，构造类别掩码向量$p_{\text{mask}} = [1, 1, \dots, 1, 0, \dots, 0] \in \mathbb{R}^{20}$，其中$1$的个数等于该算例中零件类别数
    * 对于每个零件的摆放方向，以类似方法定义$o_{\text{mask}}^{(i)} = [1, 1, \dots, 1, 0, \dots, 0] \in \mathbb{R}^{7}$，然后将所有的向量堆叠起来：$$O_{\text{mask}} = \left[ \begin{matrix}1 & \cdots & 1 & 0 & \cdots &  0\\1 & \cdots & 1 & 0 & \cdots &  0\\\vdots & \vdots & \vdots & \vdots & \vdots & \vdots\\1 & \cdots & 1 & 0 & \cdots &  0\end{matrix} \right] \in \mathbb{R}^{n_{\text{parts}} \times 7}$$
    * 由于矩阵$O_{\text{mask}}$的形状会随着算例中类别数量改变而改变，因此可不断填$0$至矩阵形状固定为$20 \times 7$: $$O_{\text{mask}} = \left[ \begin{matrix}1 & \cdots & 1 & 0 & \cdots &  0\\1 & \cdots & 1 & 0 & \cdots &  0\\\vdots & \vdots & \vdots & \vdots & \vdots & \vdots\\1 & \cdots & 1 & 0 & \cdots &  0 \\\vdots & \vdots & \vdots & \vdots & \vdots & \vdots\\ 0 & 0 & 0 & 0 & 0 & 0\end{matrix} \right] \in \mathbb{R}^{20 \times 7}$$
* Batch的开放机制
    * 每次只开放一个Batch，网络输出的零件种类分布决定将何种零件填入该Batch
    * 如果无法填入该Batch，则关闭此Batch，打开新的Batch（apply negative reward）
* 其他变量
    * Batch参数，存储该Batch中的占用情况、含有的所有零件的种类、位置和打印方向（便于计算打印时间和能耗）



**每次看见一个批次，旧批次满后关闭，直至所有零件全部被分配**
* 输出：$[0.5, 0.2, 0.3, 0.5, 0.6, ]$
* 若装箱失败则

* 输入：每个零件的状况
* 输出：每种零件被选择的概率
* 不考虑batch数量

![](Pasted%20image%2020240801143631.png)



---

